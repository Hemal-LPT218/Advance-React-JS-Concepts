export enum ROUTES_URL {
  ADMIN_HOME = "/admin",
  STUDENT_HOME = "/student",
  OTHER = "*",
  LOGIN = "/login",
  REGISTER = "/register",
}

export enum ACCOUNT_TYPE {
  ADMIN = "Admin",
  STUDENT = "Student",
}
